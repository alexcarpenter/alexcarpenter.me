---
import type { CollectionEntry } from "astro:content";
import { getCollection } from "astro:content";
import BaseLayout from "@/layouts/BaseLayout.astro";
import SectionBreak from "@/components/SectionBreak.astro";
import SectionLabel from "@/components/SectionLabel.astro";
import CollectionRoot from "@/components/Collection/Root.astro";
import CollectionItem from "@/components/Collection/Item.astro";

const smartHomeSlugs: CollectionEntry<"gear">["slug"][] = [
  "apple-home-pod-mini",
  "aqara-u100-smart-lock",
  "aqara-fp2-presence-sensor",
  "aqara-wireless-mini-switch",
  "philips-hue-smart-plug",
  "lutron-aurora-smart-bulb-dimmer-switch",
];

const items = await getCollection("gear", (item) => {
  return smartHomeSlugs.includes(item.slug);
}).then((filteredItems) => {
  return filteredItems.sort((a, b) => {
    return smartHomeSlugs.indexOf(a.slug) - smartHomeSlugs.indexOf(b.slug);
  });
});
---

<BaseLayout
  title="Smart Home"
  description="Lorem ipsum dolor sit amet consectetur adipisicing elit. Harum officiis aliquam architecto eaque."
>
  <header class="relative px-4 py-12 text-center sm:px-8 sm:py-16">
    <SectionLabel as="p">Intro</SectionLabel>
    <h1 class="font-mono text-sm text-neutral-400">Smart Home</h1>
    <p class="mt-2 text-balance text-xl font-medium tracking-tight sm:text-2xl">
      Lorem ipsum dolor sit amet consectetur adipisicing elit. Harum officiis
      aliquam architecto eaque.
    </p>
  </header>

  <SectionBreak />

  <section class="relative flex flex-1 flex-col">
    <SectionLabel>Items</SectionLabel>

    <CollectionRoot>
      {
        items.map((item) => {
          return (
            <CollectionItem>
              <p class="mb-1 font-mono text-xs uppercase text-orange-300">
                {item.data.eyebrow}
              </p>
              <h3 class="font-semibold tracking-tight">{item.data.title}</h3>
              {/* <p class="mt-4 text-neutral-400">{item.data.description}</p> */}
            </CollectionItem>
          );
        })
      }
    </CollectionRoot>
  </section>
</BaseLayout>
